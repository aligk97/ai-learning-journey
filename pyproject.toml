[project]
name = "ai-learning-journey"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "aligk97", email = "aligokmen.kudas@outlook.com" }
]
requires-python = ">=3.9"
dependencies = [
    "matplotlib>=3.9.4",
    "pandas>=2.3.3",
    "scikit-learn>=1.6.1",
]

[project.scripts]
ai-learning-journey = "ai_learning_journey:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
